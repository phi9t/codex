apiVersion: v1
kind: Service
metadata:
  name: dynamo-frontend
  namespace: codex-inference
  labels:
    app.kubernetes.io/name: dynamo-frontend
    app.kubernetes.io/part-of: codex-inference
  annotations:
    prometheus.io/scrape: "true"
    prometheus.io/port: "8080"
    prometheus.io/path: "/metrics"
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: dynamo-frontend
  ports:
    - name: http
      port: 8080
      targetPort: http
---
# Use LoadBalancer for external access (or configure Ingress separately)
# Uncomment below and comment out the ClusterIP service above if needed.
#
# apiVersion: v1
# kind: Service
# metadata:
#   name: dynamo-frontend-external
#   namespace: codex-inference
#   labels:
#     app.kubernetes.io/name: dynamo-frontend
# spec:
#   type: LoadBalancer
#   selector:
#     app.kubernetes.io/name: dynamo-frontend
#   ports:
#     - name: http
#       port: 8080
#       targetPort: http
