apiVersion: v1
kind: Service
metadata:
  name: etcd-headless
  namespace: codex-inference
  labels:
    app.kubernetes.io/name: etcd
    app.kubernetes.io/part-of: codex-inference
spec:
  clusterIP: None
  selector:
    app.kubernetes.io/name: etcd
  ports:
    - name: client
      port: 2379
---
apiVersion: v1
kind: Service
metadata:
  name: etcd
  namespace: codex-inference
  labels:
    app.kubernetes.io/name: etcd
    app.kubernetes.io/part-of: codex-inference
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: etcd
  ports:
    - name: client
      port: 2379
